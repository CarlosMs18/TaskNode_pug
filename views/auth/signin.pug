<!DOCTYPE html>
html(lang="en")
    head
        include ../partials/headers
    body.login
        .contenedor-formulario
            h1 Iniciar Sesión en UpTask
            
            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error

            form#formulario.caja.caja-login(method="POST" action="/auth/signin")
                .campo
                    label(for="email") E-mail:
                    input(type="text" name="email" placeholder="Tu Email" )
                .campo
                    label(for="password") Password:
                    input(type="password" name="password" placeholder="Tu Password" )
                .campo.enviar
                    input.boton(type="submit" value="Iniciar Sesión")
                .campo.acciones
                    a(href="/auth/signup") ¿Aún no tienes cuenta? Crea una gratis
                    a(href="/reestablecer") ¿Olvidaste tu Contraseña?